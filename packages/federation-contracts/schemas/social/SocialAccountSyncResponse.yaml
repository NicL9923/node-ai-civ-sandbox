type: object
title: SocialAccountSyncResponse
description: Accounts after an atomic sync, in request order.
additionalProperties: false
required:
  - accounts
properties:
  accounts:
    type: array
    minItems: 1
    maxItems: 100
    items:
      $ref: ./SocialAccount.yaml
